# Prompt Firewall — HSU AI-Driven Challenge 2026
# Phiên bản đã kiểm chứng thực tế trên macOS arm64 / Python 3.9.6.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Chạy chấm điểm:
#   python artifacts/predict.py input.csv output.csv [text_column]

# --- bắt buộc để chạy predict.py ---
torch==2.8.0
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==0.36.2
scikit-learn==1.6.1
scipy==1.13.1
numpy==2.0.2
pandas==2.3.3
joblib==1.5.3

# --- chỉ cần để chạy lại notebook (huấn luyện + EDA) ---
datasets==4.5.0
matplotlib==3.9.4
ipykernel==6.31.0
jupyter_client==8.6.3
nbformat==5.10.4
